u8* in = 0x10 // Input device
u8* out = 0x11 // Output device

void putc(u8 c)
{
	*out = c
}

void putu(u64 n)
{
	if (n < 10) {
		putc(u8(n + '0'))
	} else {
		putu(n / 10)
		putc(u8(n % 10 + '0'))
	}
}

u64 main()
{
	i8[5] arr = { 1, 2, 3, 4, 5 }
	putu(*(arr + 1))
}