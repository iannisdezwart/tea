#include "trick-the-vscode-c-syntax-highlighter.h"

// i32 fn(u64 a, u64 b)
// {
// 	i32 c = a * b / 2;
// 	return c;
// }

// i32 main()
// {
// 	return fn(5, 7);
// }

u64 multiply(u64 a, u64 b)
{
	u64 ans = a * b;
	return ans;
}

u64 add(u64 a, u64 b)
{
	u64 ans = a + b;
	return ans;
}

u64 main()
{
	return 1 - 2;
}