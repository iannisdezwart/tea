u8 *in = 0x10 // Input device
u8 *out = 0x11 // Output device

void putc(u8 c)
{
	*out = c
}

class TwoChars
{
	u8 a
	u8 b

	void print()
	{
		putc(a)
		putc(' ')
		putc(b)
		putc('\n')
	}
}

u64 main()
{
	TwoChars chars = { 'a', 'b' }
	chars.print()
}