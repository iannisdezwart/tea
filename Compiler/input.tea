u8 *in = 0x10 // Input device
u8 *out = 0x11 // Output device

void putc(u8 c)
{
	*out = c
}

void puts(u8 *c, u64 len)
{
	u64 i = 0

	while (i < len)
	{
		putc(*c++)
		i++
	}
}

u64 main()
{
	u8 *str = "Hello, World!"
	puts(str, 13)
}