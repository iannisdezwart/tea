#ifndef TEA_UTIL_HEADER
#define TEA_UTIL_HEADER

#include <bits/stdc++.h>

#define err(message, ...) do { \
	fprintf(stderr, message, ##__VA_ARGS__); \
	putc('\n', stderr); \
	exit(0); \
} while (0)

using namespace std;

#endif